1. Inside the pod — start Jupyter
Run this inside your pod:

jupyter lab \
  --ip=127.0.0.1 \
  --port=8888 \
  --no-browser \
  --allow-root \
  --ServerApp.token='' \
  --ServerApp.password=''


2. On your local machine — port forward
In another terminal on your local machine:

kubectl port-forward -n wenglab-vs pod/<your-pod-name> 8888:8888

3. Open in browser

http://localhost:8888